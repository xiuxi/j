366 Floating-Point Arithmetic
Figure 10.16 displays the design for the floating to ASCII procedure. After the
leading space or minus sign is generated, most of the work necessary to get the remaining characters is done before they are actually produced. The value is repeatedly multiplied or divided by 10 until it is at least 1.0 but less than 10.0. Multiplication is used if the
point at first destination byte;
if value ≤ 0
then
put blank in destination string;
else
put minus in destination string;
value := –value;
end if;
point at next destination byte;
exponent := 0;
if value ( 0
then
if value > 10
then
until value < 10 loop
divide value by 10;
add 1 to exponent;
end until;
else
while value < 1 loop
multiply value by 10;
subtract 1 from exponent;
end while;
end if;
end if;
(continued)
Figure 10.16 Floating-point to ASCII conversion algorithm
TEAM LinG - Live, Informative, Non-cost and Genuine!
